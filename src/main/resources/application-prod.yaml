logging:
  level:
    root: INFO
    org.springframework.web: WARN
    com.invex.examen: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n"
  file:
    name: logs/app-prod.log

